language: ruby
before_install:
  - bundle install
rvm:
  - 2.0.0
  - 1.9.3
script:
  - bundle exec jekyll build
  - bundle exec rake coveralls
notifications:
  email:
    on_success: never
    on_failure: never
