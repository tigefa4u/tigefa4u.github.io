language: ruby
before_install:
  - gem install rake
  - bundle install
rvm:
  - 2.1.1
  - 2.0.0
  - 1.9.3
  - 1.9.1
  - 1.8.7
script:
  - bundle exec jekyll build
  - rake coveralls
notifications:
  irc:
    on_success: change
    on_failure: change
    channels:
      - "irc.freenode.org#tigefa"
    template:
      - "%{repository}#%{build_number} (%{branch}) %{message} %{build_url}"
  email:
    on_success: never
    on_failure: never
