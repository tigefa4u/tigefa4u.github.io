machine:
  timezone:
    Asia/Jakarta
  ruby:
    version: 2.1.5

dependencies: 
  pre:
    - gem update --system
    - gem install bundler --conservative
    - bundle install

test:
  post:
    - bundle exec jekyll build
