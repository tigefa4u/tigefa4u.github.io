machine:
  timezone:
    Asia/Jakarta
  ruby:
    version: 2.3.3
  python:
    version: 2.7.13

dependencies:
  pre:
    - pip install -U pygments speedtest-cli
    - gem update --system
    - gem install bundler --pre
    - bundle install
    - speedtest --secure --share

test:
  post:
#    - bundle exec travis lint
    - bundle exec jekyll build
