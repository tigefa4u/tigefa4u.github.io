machine:
  timezone:
    Asia/Jakarta
  ruby:
    version: 2.1.5
  python:
    version: 2.7.10

dependencies: 
  pre:
    - curl -k -L -o get-pip.py https://bootstrap.pypa.io/get-pip.py
    - python get-pip.py
    - pip install -U pygments
    - gem update --system
    - gem install bundler --conservative
    - bundle install

test:
  post:
    - bundle exec jekyll build
